export default class ExtratoModel {
  constructor(id, tipo, quantidade, valor, data) {
    this.id = id;
    this.tipo = tipo;
    this.quantidade = quantidade;
    this.valor = valor; // número
    this.data = data;
  }
}
